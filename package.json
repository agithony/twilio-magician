{
  "name": "twilio-magician",
  "private": true,
  "scripts": {
    "dev": "concurrently --names server,client -c blue,magenta \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "build": "pnpm --filter client build && pnpm --filter server build",
    "start": "pnpm --filter server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@tsparticles/engine", "esbuild"]
  }
}
